### PYTHON CODE FOR MERGING TIFF FILES INTO ONE FILE
# To see which packages are installed in your current conda environment and their version numbers
conda list

# To see which Python installation is currently set as the default
which python

# To see if the conda installation of Python is in your PATH variable
echo $PATH

# Create condo environment
#conda create --name myenv
# Say ‘yes’ to imitate it
#y
# Create condo environment with my python version
conda create -n myenv python=2.7.14 

# Get inför on your condo environment
conda info --envs

# Activate your conda environment
source activate myenv

# Install the necessary package ‘gdal’ in your conda environment
conda install gdal

# Say ‘yes’ to installation
y

# Execute actual merge of tiff files
gdal_merge.py -o /Users/user1/Documents/output.tif /Users/user1/Documents/test/*.tif

# Remove condo environment after having done the merge
conda remove --name myenv --all
